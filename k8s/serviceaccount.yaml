apiVersion: v1
kind: ServiceAccount
metadata:
  name: authentication-service
  namespace: authentication
  labels:
    app.kubernetes.io/name: authentication-service
    app.kubernetes.io/instance: production
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/component: security
    app.kubernetes.io/part-of: authentication-platform
    app.kubernetes.io/managed-by: kubernetes
automountServiceAccountToken: false

---
# Optional: Role and RoleBinding for service account permissions
# Uncomment if your service needs specific Kubernetes API access
# apiVersion: rbac.authorization.k8s.io/v1
# kind: Role
# metadata:
#   name: authentication-service
#   namespace: authentication
# rules:
# - apiGroups: [""]
#   resources: ["configmaps", "secrets"]
#   verbs: ["get", "list"]

# ---
# apiVersion: rbac.authorization.k8s.io/v1
# kind: RoleBinding
# metadata:
#   name: authentication-service
#   namespace: authentication
# subjects:
# - kind: ServiceAccount
#   name: authentication-service
#   namespace: authentication
# roleRef:
#   kind: Role
#   name: authentication-service
#   apiGroup: rbac.authorization.k8s.io