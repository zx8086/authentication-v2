# Dependencies
node_modules/
bun.lockb

# Environment variables (only .env.example is tracked)
.env
.env.*
!.env.example

# Build outputs
dist/
build/

# Test coverage
coverage/

# Test results
test/results/
test-results/
playwright-report/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Temporary files
tmp/
temp/

# Baseline timing artifacts and backups (generate fresh each time)
baseline-timings.txt
package.json.backup

# Bundled runtime executables (built locally or in CI)
scripts/bundled-runtimes/

# CLAUDE.md
k8s
docs/MIGRATION_GAPS.md
.stryker-tmp

# Stryker experimental configs (only stryker.config.json is tracked)
stryker.config.*.json
stryker.*.config.json
stryker.config copy.json

# Profiling outputs (keep baselines tracked in git)
profiles/current/
profiles/archive/
!profiles/baselines/.gitkeep

# Kong decK configuration (contains credentials)
/kong/
